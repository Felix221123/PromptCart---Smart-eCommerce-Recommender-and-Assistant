# LIST OF THINGS TO DO IN FRONTEND
  -- Create components like buttons, headers, text_description ,inputs(with the labels as props) , card containers etc
  -- Create safe context for when users log in and register